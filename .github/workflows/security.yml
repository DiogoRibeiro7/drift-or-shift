name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly, so newly disclosed advisories surface without waiting for a push.
    - cron: "17 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: github/codeql-action/init@v3
        with:
          languages: python
          queries: security-and-quality

      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:python"

  audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install the project
        run: python -m pip install -e ".[dev]"

      - name: Audit installed dependencies for known advisories
        run: |
          python -m pip install pip-audit
          # --skip-editable leaves out this project itself, which is not on
          # PyPI and so can never be audited. Not --strict: with an editable
          # install present it fails on that alone. Real advisories still exit
          # non-zero without it.
          python -m pip_audit --skip-editable --progress-spinner off

  zizmor:
    name: Workflow lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Audit the GitHub Actions workflows themselves
        run: |
          python -m pip install zizmor
          # zizmor ships a binary, not an importable module.
          # `|| true` keeps this advisory: findings land in the Security tab
          # rather than blocking a merge.
          zizmor --config .github/zizmor.yml --format sarif \n            .github/workflows > zizmor.sarif || true

      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: zizmor.sarif
          category: zizmor
